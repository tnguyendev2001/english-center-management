package com.englishcenter.makeupcredit;

/** Why an approved-leave tracking record was created. Not an extra class session. */
public enum MakeupCreditReason {
    EXCUSED_ABSENCE,
    CLASS_CANCELED,
    MANUAL_ADJUSTMENT
}
