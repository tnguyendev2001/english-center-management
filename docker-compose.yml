services:
  mysql:
    image: mysql:8.4
    container_name: english_center_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: english_center
      MYSQL_USER: english_center
      MYSQL_PASSWORD: english_center
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: